% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw-results.R
\name{raw-results}
\alias{raw-results}
\alias{rawResultsSchema}
\alias{rawResultsRow}
\alias{writeRawResults}
\alias{readRawResults}
\alias{setupRawResultsFilter}
\alias{parseRawResultsParams}
\title{Canonical raw-results helpers}
\usage{
rawResultsSchema(fit)

rawResultsRow(
  fit = NULL,
  source,
  hypothesis,
  sample,
  modelLabel,
  role,
  errorMessage = NA_character_,
  objf = NULL,
  minimizationSuccessful = NULL,
  covarianceStepSuccessful = NULL,
  estimateNearBoundary = NULL,
  significantDigits = NULL,
  conditionNumber = NULL,
  theta = NULL,
  omega = NULL,
  sigma = NULL,
  se = NULL,
  schema = NULL
)

writeRawResults(rows, dir, basename = "raw_results")

readRawResults(path)

setupRawResultsFilter(filter)

parseRawResultsParams(rawres, fit, offset = 1L, filter = NULL)
}
\arguments{
\item{fit}{A fitted \code{nlmixr2} object, or a fit-like list containing \code{theta},
\code{omega}, \code{sigma}, and related metadata.}

\item{source}{Canonical producer name such as \code{"bootstrap"}, \code{"sir"}, or
\code{"sse"}.}

\item{hypothesis}{Hypothesis label recorded in the raw-results row.}

\item{sample}{Integer replicate index. Use \code{0} for a reference row.}

\item{modelLabel}{Short model label stored in \code{model_label}.}

\item{role}{Role label stored in \code{role}.}

\item{errorMessage}{Optional error message recorded for failed fits.}

\item{objf}{Optional objective-function value override.}

\item{minimizationSuccessful, covarianceStepSuccessful, estimateNearBoundary}{Optional diagnostic flag overrides. When \code{NULL}, \code{rawResultsRow()} uses
simple heuristics based on the supplied fit.}

\item{significantDigits}{Optional significant-digits override.}

\item{conditionNumber}{Optional condition-number override.}

\item{theta, omega, sigma}{Optional parameter overrides. \code{theta} may be a
named vector or an unnamed vector in schema order. \code{omega} and \code{sigma}
may be named vectors, unnamed vectors in schema order, or full matrices.}

\item{se}{Optional standard-error overrides in parameter-column order.}

\item{schema}{Optional schema list from \code{\link[=rawResultsSchema]{rawResultsSchema()}}. Supply this to
target a wider union schema than the one implied by \code{fit}.}

\item{rows}{Data frame of canonical raw-results rows.}

\item{dir}{Directory where the CSV, RDS, and JSON sidecar should be written.}

\item{basename}{Basename used for the output files.}

\item{path}{Path to a canonical raw-results CSV, RDS, or containing
directory.}

\item{filter}{Either a PsN-style character filter, a one-sided formula, or
an unevaluated expression.}

\item{rawres}{A raw-results data frame, a path understood by
\code{\link[=readRawResults]{readRawResults()}}, or the result of \code{\link[=readRawResults]{readRawResults()}}.}

\item{offset}{Integer sample offset. The default \code{1L} skips the canonical
reference row with \code{sample = 0}.}
}
\value{
\code{rawResultsSchema()} returns a list with \code{columns}, \code{baseCols},
\code{thetaCols}, \code{omegaCols}, \code{sigmaCols}, \code{seCols}, and \code{schemaVersion}.

\code{rawResultsRow()} returns a one-row data frame in canonical schema
order.

\code{writeRawResults()} invisibly returns a list containing the canonical
data frame and the three output paths.

\code{readRawResults()} returns the raw-results data frame with the parsed
header attached as the \code{rawResultsHeader} attribute.

\code{setupRawResultsFilter()} returns a predicate function that accepts a
raw-results data frame and returns a logical inclusion vector.

\code{parseRawResultsParams()} returns a named list of per-sample
parameter sets, each containing \code{sample}, \code{source}, \code{hypothesis},
\code{modelLabel}, \code{role}, \code{theta}, \code{omega}, and \code{sigma}.
}
\description{
These helpers define the shared per-fit raw-results schema used by the
\code{nlmixr2} extension packages. Writers emit the canonical column order,
optional standard-error columns, and a JSON sidecar describing the block
boundaries so downstream readers do not need to re-parse parameter labels.
}
\section{Lifecycle}{

Stable.
}

