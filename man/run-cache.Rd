% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-cache.R
\name{run-cache}
\alias{run-cache}
\alias{resolveRunDir}
\alias{writeRunState}
\alias{readRunState}
\alias{taskCache}
\alias{pendingTasks}
\alias{withRunSeed}
\alias{deriveFitName}
\title{Shared run-cache helpers}
\usage{
resolveRunDir(prefix, fitName, restart, outputDir = NULL)

writeRunState(dir, state, schema)

readRunState(dir, schema)

taskCache(dir, key = NULL)

pendingTasks(allKeys, completedKeys)

withRunSeed(dir, seed = NULL, key = NULL, prefix = NULL, expr)

deriveFitName(expr, maxLength = 50L)
}
\arguments{
\item{prefix}{Optional seed-file prefix override. Supply this when you need a
stable artifact name such as \code{"boot"} or \code{"sir"} regardless of the
directory basename.}

\item{fitName}{Base fit name used when constructing numbered output
directories.}

\item{restart}{Logical flag controlling resume versus fresh-run behavior.
With \code{outputDir = NULL}, \code{restart = FALSE} resumes the latest numbered
directory when one exists; otherwise a new numbered directory is selected.
With an explicit \code{outputDir}, \code{restart = TRUE} marks an existing directory
for overwrite.}

\item{outputDir}{Optional explicit output directory.}

\item{dir}{Run directory containing the state file.}

\item{state}{Arbitrary R object to persist.}

\item{schema}{Either a prefix string, for example \code{"sse"}, or a list with
\code{prefix} and optional \code{version}.}

\item{key}{Optional cache namespace used to isolate one family of artifacts
inside a run directory.}

\item{allKeys}{Full set of task keys.}

\item{completedKeys}{Completed task keys.}

\item{seed}{Optional explicit master seed. When \code{NULL}, the helper derives a
stable integer seed from the current \code{.Random.seed} if one exists, and
persists it for later resumes.}

\item{expr}{Optional expression to evaluate under a derived per-key seed.}

\item{maxLength}{Maximum length of the returned fit name.}
}
\value{
\code{resolveRunDir()} returns a list with \code{path}, \code{mode}, \code{created}, and
\code{prefix}.

\code{writeRunState()} invisibly returns the state-file path;
\code{readRunState()} returns the saved state or \code{NULL} when the file does not
exist.

\code{taskCache()} returns a list with cache methods \code{has()}, \code{get()},
\code{put()}, and \code{keys()}.

\code{pendingTasks()} returns the ordered subset of \code{allKeys} that does
not appear in \code{completedKeys}.

With no \code{key}, \code{withRunSeed()} returns the persisted master seed.
With \code{key} and no \code{expr}, it returns the derived per-key seed. With both
\code{key} and \code{expr}, it evaluates \code{expr} under that derived seed and restores
the prior RNG state on exit.

\code{deriveFitName()} returns a sanitized single-string fit label.
}
\description{
These helpers implement the common numbered-run-directory, state-file,
task-cache, and deterministic seeding patterns used by the split
\code{nlmixr2} extension packages.
}
\section{Lifecycle}{

Stable.
}

